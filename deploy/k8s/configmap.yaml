# Minimal buoy.yaml. Replace `node.name` and add the rest of
# buoy.yaml.example's sections as needed, then `kubectl apply -f`.
apiVersion: v1
kind: ConfigMap
metadata:
  name: buoy-config
  labels:
    app.kubernetes.io/name: buoy
data:
  buoy.yaml: |
    node:
      name: my-node
