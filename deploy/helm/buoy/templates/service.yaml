apiVersion: v1
kind: Service
metadata:
  name: {{ include "buoy.fullname" . }}
  labels:
    {{- include "buoy.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "buoy.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
